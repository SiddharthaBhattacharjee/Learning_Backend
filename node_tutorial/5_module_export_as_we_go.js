module.exports.items = ['item1', 'item2']; // can export directly like this

const person = {
    name: 'bob',
    age : 20
}

module.exports.person = person; // can export directly like this

module.exports.person2 = {name:'Rob', age:'22'} // can export directly like this
